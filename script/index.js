// import { tableControl, formControl } from './modules/control.js';
import { renderTodoApp } from './modules/render.js';
// import { getStorage } from './modules/serviceStorage.js';

// const data = [];

const init = () => {
  // const app = document.querySelector(selectorApp);
  // renderTodoApp();
  // const { name, form, list } = renderTodoApp(title);
  // const data = getStorage(name);

  // renderTask(list, data);
  // formControl(form, list, name);
  // delTaskPage(data, list, name);
  // addTaskData(data);
  renderTodoApp();
};
// window.todoInit = init;
init();
